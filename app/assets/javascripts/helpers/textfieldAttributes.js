Ember.TextField.reopen({
    
    attributeBindings:  ['required'],
    
    required:           null
});

Ember.Checkbox.reopen({
    
    attributeBindings:  ['required'],
    
    required:           null
});